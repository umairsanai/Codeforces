#include <bits/stdc++.h>
using namespace std;

int main() {
  system("cls");
  int t;
  cin >> t;
  while(t--) {
    int n, m, q, t1, t2, d, moves = 0;
    cin >> n >> m >> q >> t1 >> t2 >> d;
    if (d < t1) {
      moves = t1 - 1;
    } else if ( d > t2) {
      moves = 
    }

  }

  
  return 0;
}